// Local config example for development
// Copy this file to config.js and add your real API key
// config.js is in .gitignore so it won't be committed

window.CONFIG = {
    API_KEY: 'YOUR_OPENWEATHER_API_KEY_HERE'
}
